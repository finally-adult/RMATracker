@model RMATracker.Models.RMA

@{
    ViewData["Title"] = "AddRMA";
}

<h1>AddRMA</h1>

<h4>RMA</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddRMA">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RMANumber" class="control-label"></label>
                <input asp-for="RMANumber" class="form-control" />
                <span asp-validation-for="RMANumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
